<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_record_route">

    <fragment
        android:id="@+id/nav_record_route"
        android:name="pl.jakubokrasa.bikeroutes.features.routerecording.ui.RecordRouteFragment"
        android:label="@string/title_record_route"
        tools:layout="@layout/fragment_record_route" />

    <fragment
        android:id="@+id/navigation_dashboard"
        android:name="pl.jakubokrasa.bikeroutes.temporary.ui.dashboard.DashboardFragment"
        android:label="@string/title_dashboard"
        tools:layout="@layout/fragment_dashboard" />

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="pl.jakubokrasa.bikeroutes.temporary.ui.settings.SettingsFragment"
        android:label="@string/title_notifications"
        tools:layout="@layout/fragment_notifications" />
</navigation>